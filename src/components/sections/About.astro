---
import Section from "../Section.astro";

import { basics } from "@cv";
const { summary } = basics;

import { getI18N } from "../../i18n/";

const { currentLocale } = Astro;
const i18n = getI18N({ currentLocale });
---

<Section title={i18n.aboutTitle}>
  <p>{i18n.summary}</p>
</Section>
